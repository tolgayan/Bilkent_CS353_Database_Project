<html>
  <head> </head>
  <body>
    <p>All news will be seen here</p>

    <a href="/news/addpost">
      <button>
        Add New Post
      </button>
    </a>

    <% if(all_news.length!=0){ %> <% all_news.forEach(function(news) { %>
    <h1><%= news.title %></h1>
    <h3><%= news.name %></h3>
    <p><%= news.category %></p>
    <p><%= news.content %></p>
    <% }) %> <% } %>
  </body>
</html>
